#a cluster manager manage multiple easywalkers which are cutting-corner walker
#cluster manager are responsible for database operation which easywalker doesn't have
#cluster manager also receiving signals from master node (my laptop) and operate as the signal indicates.

class ClusterManager(object):
    def __init__(self):
        pass